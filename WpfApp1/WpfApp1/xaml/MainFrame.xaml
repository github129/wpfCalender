<Window x:Class="WpfApp1.MainFrame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainFrame" Height="750" Width="600">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:SomeCalenderWindowViewModel}">
            <local:SomeCalenderPageControl/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:OneCalenderPageControlViewModel}">
            <local:OneCalenderPageControl/>
        </DataTemplate>
    </Window.Resources>
    <Window.DataContext>
        <local:MainFrameViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ContentControl DockPanel.Dock="Bottom">
            <Button Content="表示切替" HorizontalAlignment="Stretch" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        </ContentControl>
        <DockPanel LastChildFill="True" Grid.Row="1">
            <ContentControl DockPanel.Dock="Bottom" Content="{Binding CurrentPage}"/>
        </DockPanel>
    </Grid>
</Window>
